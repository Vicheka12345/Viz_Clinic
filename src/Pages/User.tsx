import React from 'react';
import PageTemplate from '../Components/PageTemplate';

const User = () => (
  <PageTemplate 
    title="User Profile" 
    description="View and update your health profile and history." 
  />
);

export default User;
